{
  "name": "newproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rimraf build && tsc-watch --noClear --onFirstSuccess tsc-alias --onSuccess \"node build\"",
    "clean": "rimraf build && rimraf logs",
    "build": "rimraf build && tsc && tsc-alias",
    "start": "node build",
    "prettier:fix": "prettier --config .prettierrc.json --write src/**/**/*.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
   
  },
  "devDependencies": {
    
  },
  "_moduleAliases": {
    "@": "build"
  }
}
